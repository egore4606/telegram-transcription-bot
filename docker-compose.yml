services:
  bot:
    build: .
    env_file: .env
    restart: unless-stopped
    environment:
      DATABASE_PATH: /data/bot.sqlite3
    volumes:
      - bot_data:/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "30"

volumes:
  bot_data:
